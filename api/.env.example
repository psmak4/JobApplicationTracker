# Database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/job_tracker

# Server configuration
PORT=4000

# Allowed frontend origins (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Better Auth secrets (generate secure random values for production)
BETTER_AUTH_SECRET=your-secret-key-here
BETTER_AUTH_URL=http://localhost:4000

# Admin user IDs (comma-separated list of user IDs that have admin privileges)
# Get your user ID from the database after signing up, then add it here
ADMIN_USER_IDS=
